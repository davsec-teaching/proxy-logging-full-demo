A Redis-based logging framework. You can annotate a method as `@Log` and it will log all invocations of that method in the Redis db. 

Run `mvn exec:java` to run. Make sure `redis-server` is running. 
